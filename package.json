{
  "name": "projectflow",
  "version": "1.0.0",
  "description": "ProjectFlow - A project and task management system",
  "main": "server/startup.js",
  "scripts": {
    "start": "cd server && node startup.js",
    "dev": "cd client && npm start",
    "dev:server": "cd server && npm run dev",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}